{
  "rules": {
    ".read": true,
    ".write": true,

    "activity_log": {
      "//": "Index on key enables efficient limitToLast queries (the app fetches only the newest N entries instead of the whole log). Without this index, Firebase still returns results but logs a performance warning and is less efficient.",
      ".indexOn": ".key"
    },

    "projects": {
      ".indexOn": ["createdAt", "workflowStatus"]
    },

    "users": {
      ".indexOn": ["name", "role"]
    }
  }
}
